/**
 * http server settings.
 */
{
   /* HTTP concurrent connections. */
   "backlog": null
   /* Nio buffer length. */
   ,"byteBufferLength": null
   /* Socket send buffer length. */
   ,"socketSendBuffer": null
   /* Socket receive buffer length. */
   ,"socketReceiveBuffer": null
   /* Server bind address. */
   ,"localAddress": null
   /* Server bind port. */
   ,"localPort": null
   /* number of worker threads. */
   ,"workerThread": null
   /* Compile cache size. */
   ,"compileCacheSize": null
   /* Compile cache root directory. */
   ,"compileCacheRootDir": null
   /* Enables/disables the cache mode for static file requests. */
   ,"sendFileCacheMode": null
}
